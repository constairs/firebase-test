import styled from 'styled-components';

import { Page } from '../../components/UI/Page';
import { Button } from '../../components/UI/Button';

export const StyledIssuePage = styled(Page)`
  ${Button} {
    width: auto;
  };
`;
